{
  "name": "crypto-profiles",
  "version": "0.0.1",
  "description": "A JSON profile system where objects are cryptographically signed and reference one-another",
  "main": "lib/index",
  "scripts": {
    "compile": "babel --presets es2015 src -d lib",
    "test": "npm run compile; node lib/testing/unitTests.js",
    "prepublish": "npm run compile"
  },
  "author": "Halfmoon Labs, Inc.",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-es2015": "^6.3.13",
    "tape": "^4.4.0"
  },
  "dependencies": {
    "dateformat": "^1.0.12",
    "dns-zonefile": "^0.1.12",
    "elliptic-curve": "^0.1.0",
    "elliptic-keychain": "0.0.1",
    "jwt-js": "^0.5.0"
  }
}
